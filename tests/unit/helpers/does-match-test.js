import { doesMatch } from '../../../helpers/does-match';
import { module, test } from 'qunit';

module('Unit | Helper | does match');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = doesMatch(42);
  assert.ok(result);
});
